# Football Match Simulator

A pure Java football (soccer) match simulation engine.

## Features
- Configurable match simulation with team tactics and player ratings
- Event-driven engine (goals, cards, penalties, offsides, substitutions)
- Pluggable rule engine with triggers and actions for dynamic match rules
- Position-based player rating calculators

## Running
Compile the `src` directory and run `Main`.
